apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.client.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.client.name }}
spec:
  replicas: {{ .Values.client.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.client.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.client.name }}
    spec:
      serviceAccountName: {{ .Values.serviceAccount.name }}
      securityContext:
        {{- toYaml .Values.securityContext | nindent 8 }}
      containers:
      - name: {{ .Values.client.name }}
        image: {{ .Values.global.registry }}/{{ .Values.client.image.repository }}:{{ .Values.client.image.tag }}
        imagePullPolicy: {{ .Values.client.image.pullPolicy }}
        ports:
        - containerPort: {{ .Values.client.port }}
          name: http
        env:
        - name: API_BACKEND_URL
          value: http://{{ .Values.server.name }}/api/v1
        resources:
          {{- toYaml .Values.client.resources | nindent 10 }}
        livenessProbe:
          httpGet:
            path: /
            port: {{ .Values.client.port }}
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /
            port: {{ .Values.client.port }}
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 2
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.client.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.client.name }}
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: {{ .Values.client.port }}
    protocol: TCP
    name: http
  selector:
    app: {{ .Values.client.name }}
